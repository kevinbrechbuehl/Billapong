<Window x:Class="Billapong.GameConsole.Views.GameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:Billapong.GameConsole.Views"
        Title="Billapong" SizeToContent="WidthAndHeight" WindowStyle="None">

    <ItemsControl ItemsSource="{Binding Holes}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <views:GameFieldCanvas Background="White" Width="300" Height="300" ClickCommand="{Binding GameFieldClickedCommand}" />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Ellipse Width="{Binding Diameter}" Height="{Binding Diameter}" Fill="Black" />
            </DataTemplate>
        </ItemsControl.ItemTemplate>
        <ItemsControl.ItemContainerStyle>
            <Style>
                <Setter Property="Canvas.Left" Value="{Binding Left}" />
                <Setter Property="Canvas.Top" Value="{Binding Top}" />
            </Style>
        </ItemsControl.ItemContainerStyle>
    </ItemsControl>
</Window>
