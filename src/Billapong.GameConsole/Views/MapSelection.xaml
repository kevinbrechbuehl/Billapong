<UserControl x:Class="Billapong.GameConsole.Views.MapSelection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>

        <ListView ItemsSource="{Binding Maps}" x:Name="MapList" DockPanel.Dock="Top" Height="auto">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel IsItemsHost="True" Width="480" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding Name}" Command="{Binding ElementName=MapList, Path=DataContext.OpenWindowSelectionCommand}" CommandParameter="{Binding}" Height="60" Width="105" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Height="20">
            <Button Command="{Binding BackToMenuCommand}">Back to menu</Button>
            <Button Command="{Binding OpenWindowSelectionCommand}">Choose windows</Button>
        </StackPanel>
    </DockPanel>
</UserControl>
